# 3DGS 点云处理工具 (3dgs-pp) 产品需求文档

## 1. 项目概述

### 1.1 项目名称
3DGS Point Cloud Processing Tool (3dgs-pp)

### 1.2 项目背景
3D Gaussian Splatting (3DGS) 是一种新型的三维重建技术，其生成的点云数据通常存储在 PLY 文件中。随着场景规模增大，PLY 文件的体积也会急剧增长，给数据查看、处理和传输带来挑战。本工具旨在提供高效的 3DGS PLY 文件处理能力，支持懒加载、终端浏览和空间分块功能。

### 1.3 项目目标
- 提供高效的大型 PLY 文件懒加载机制
- 支持终端交互式浏览高斯点数据
- 实现灵活的空间分块导出功能
- 支持按条件过滤高斯椭球，清理异常或不需要数据
- 支持高斯椭球下采样，减少模型复杂度
- 支持坐标平移变换，方便点云对齐和中心化
- 支持轴变换（轴对换/镜像与 nπ/2 旋转），快速改变点云朝向

## 2. 功能需求

### 2.1 懒加载 PLY 文件读取

**功能描述**：支持读取 3DGS 格式的 PLY 文件，采用懒加载方式，避免一次性加载全部数据到内存。

**详细需求**：
- 支持标准 3DGS PLY 文件格式解析
- 仅在需要时加载具体高斯点数据
- 支持文件元数据（顶点数量、属性列表等）的快速读取
- 内存占用控制：元数据加载后内存占用 < 10MB
- 支持超大文件（> 10GB）的处理

**PLY 属性列表**（3DGS 标准）：
- x, y, z：位置坐标
- f_dc_0, f_dc_1, f_dc_2：球面调和基直流分量
- f_rest_0 到 f_rest_44：球面调和基剩余分量（45个）
- opacity：不透明度
- scale_0, scale_1, scale_2：缩放
- rot_0, rot_1, rot_2, rot_3：旋转（四元数）

### 2.2 终端列表展示与翻页

**功能描述**：在终端中以列表形式展示高斯点数据，支持上下翻页浏览。

**详细需求**：

#### 2.2.1 展示模式
- 默认展示核心属性：索引、x/y/z 坐标、opacity
- 支持展开查看更多属性（f_dc、scale、rot）
- 支持自定义选择展示的属性列
- 数值格式化：浮点数保留 6 位小数

**展开模式显示属性：**
- 索引、x、y、z、opacity
- f_dc_0, f_dc_1, f_dc_2
- scale_0, scale_1, scale_2
- rot_0, rot_1, rot_2, rot_3

#### 2.2.2 交互控制
- `j` / `↓`：向下翻一页
- `k` / `↑`：向上翻一页
- `g` / `Home`：跳转到第一页
- `G` / `End`：跳转到最后一页
- `:` / `number` + `Enter`：跳转到指定索引
- `/` + `关键词`：搜索包含特定值的点
- `e`：展开/折叠详细属性
- `q`：退出浏览模式
- `?`：显示帮助信息

#### 2.2.3 页面配置
- 每页默认显示 20 条记录
- 支持通过命令行参数自定义每页条数
- 显示当前位置：`第 X 页 / 共 Y 页 (记录 Z1-Z2 / 共 N)`

### 2.3 空间分块与导出

**功能描述**：按照指定的 X/Y/Z 方向分块数量，将点云分割成多个块，分别导出为 PLY 文件。

**详细需求**：

#### 2.3.1 分块策略
- 输入格式：`Nx*Ny*Nz`，例如 `2*3*2`
- X 方向分 Nx 块，Y 方向分 Ny 块，Z 方向分 Nz 块
- 分块边界：基于点云的包围盒（AABB）均匀划分
- 边界点处理：位于分块边界上的点归属于索引较小的块

#### 2.3.2 导出文件
- 文件命名格式：`{原文件名}_block_{i}_{j}_{k}.ply`
- 每个分块文件包含完整的 PLY 头部信息
- 保留所有原始属性数据
- 生成 `{原文件名}_block_info.json`，记录各分块的：
  - 空间范围（min/max x/y/z）
  - 点数量
  - 文件名映射

#### 2.3.3 进度反馈
- 显示整体进度百分比
- 显示已处理/总点数
- 显示已生成/总分块文件数
- 估算剩余时间

### 2.4 空间统计概览

**功能描述**：对 PLY 文件中指定参数进行统计分析，展示最大值、最小值、均值、中值、四分位数以及 5% 10% 20% 50% 90% 95% 分位数，支持快捷键切换查看不同统计维度。

**详细需求**：

#### 2.4.1 统计维度

支持对所有 PLY 数值属性进行统计，包括但不限于：

- **空间坐标**：x, y, z
- **外观属性**：opacity
- **球面调和系数**：f_dc_0, f_dc_1, f_dc_2, f_rest_0 ~ f_rest_44
- **几何属性**：scale_0, scale_1, scale_2, rot_0, rot_1, rot_2, rot_3

#### 2.4.2 统计指标

对选定属性计算以下统计值：

| 统计指标 | 说明 |
|---------|------|
| 最大值 (Max) | 数据集中的最大值 |
| 最小值 (Min) | 数据集中的最小值 |
| 均值 (Mean) | 算术平均值 |
| 中值 (Median) | 50% 分位数 |
| Q1 (25% 分位数) | 第一四分位数 |
| Q2 (50% 分位数) | 第二四分位数（同中值） |
| Q3 (75% 分位数) | 第三四分位数 |
| 5% 分位数 | 下有 5% 数据的值 |
| 10% 分位数 | 下有 10% 数据的值 |
| 20% 分位数 | 下有 20% 数据的值 |
| 50% 分位数 | 下有 50% 数据的值（同中值） |
| 90% 分位数 | 下有 90% 数据的值 |
| 95% 分位数 | 下有 95% 数据的值 |
| 标准差 (Std) | 总体标准差 |
| 偏度 (Skewness) | 分布偏斜程度 |
| 峰度 (Kurtosis) | 分布峰态程度 |

#### 2.4.3 展示模式

**模式一：单属性详细统计**

```
属性: opacity
总样本数: 52,384,129

  统计指标        值
  ──────────────────────────────
  Min             0.000123
  5% 分位数       0.003456
  10% 分位数      0.005678
  20% 分位数      0.012345
  Q1 (25%)        0.018901
  中值 (50%)      0.045678
  Q3 (75%)        0.123456
  90% 分位数      0.321654
  95% 分位数      0.487321
  Max             1.000000
  均值            0.078234
  标准差          0.112345
  偏度            4.56
  峰度            28.91
```

**模式二：多属性对比一览**

```
                     x            y            z            opacity
  ──────────────────────────────────────────────────────────────────────
  Min          -123.456      -89.012       0.123       0.000123
  5%           -98.765       -72.345       2.456       0.003456
  25%          -45.678       -12.345       12.789      0.018901
  50%            0.123         0.456       28.901      0.045678
  75%           45.789        56.123       45.678      0.123456
  95%          123.456       134.567       98.765      0.487321
  Max          156.789       167.890      112.345      1.000000
  均值           -2.345         5.678       35.901      0.078234
  标准差         78.901        82.345       45.678      0.112345
```

#### 2.4.4 交互控制

- `a` / `←`：切换到上一个属性
- `d` / `→`：切换到下一个属性
- `s`：切换展示模式（单属性详细 / 多属性对比）
- `f`：全屏统计面板，展示所有属性的关键统计值
- `o`：保存当前可见的统计信息到文件
- `q`：退出统计模式

**保存行为（`o` 快捷键）：**
- 当前为**单属性详细模式**时：保存该属性的完整统计面板（所有指标）为文本文件
- 当前为**多属性对比模式**时：保存对比面板为文本文件
- 默认保存路径：`{原文件名}_stats_{属性名}.txt`，保存在与原始 PLY 文件同目录
- 支持通过命令行参数 `--output-dir DIR` 指定输出目录
- 每次保存提示文件路径

**保存文件示例**：
```
========================================
3DGS 属性统计报告
========================================
文件: scene.ply
属性: opacity
总样本数: 52,384,129
生成时间: 2026-04-28 15:30:42
========================================

  统计指标        值
  ──────────────────────────────
  Min             0.000123
  5% 分位数       0.003456
  ...

========================================
```

#### 2.4.5 属性分布图绘制

**功能描述**：将指定参数的值分布绘制成图表并保存到指定目录，支持交互式查看和非交互式批量生成。

**详细需求**：

**图表类型：**
- **直方图 + KDE 曲线**：展示数据分布密度
- **箱线图 (Box Plot)**：展示中值、四分位数、异常值范围
- **小提琴图 (Violin Plot)**：结合直方图和箱线图，展示分布密度与四分位数

**图表元素：**
- 横轴：属性值
- 纵轴：频数 / 密度
- 在图上标注：均值（虚线）、中值（实线）、Q1/Q3（点划线）
- 标题包含：属性名、样本数
- 颜色主题：暗色背景（适合嵌入文档）

**交互触发：**
- `p`：在当前交互模式下，为当前属性绘制分布图并保存到默认目录
- `P`（Shift+p）：为所有核心属性（x, y, z, opacity, scale_0, scale_1, scale_2）批量绘制分布图

**命令行触发：**
```bash
# 为非交互模式绘制指定属性的分布图
3dgs-pp stat --attr opacity --plot <ply_file>

# 批量绘制所有核心属性
3dgs-pp stat --all --plot --output-dir ./plots <ply_file>

# 指定输出目录和图表类型
3dgs-pp stat --attr x --plot --output-dir ./charts --type box <ply_file>
```

**输出配置：**
- 默认输出目录：`{原文件名}_stats_plots/`，与原始 PLY 文件同目录
- 支持 `--output-dir DIR` 覆盖
- 文件命名格式：`{属性名}_distribution.png`
- 输出格式：PNG（默认，分辨率 300 DPI）
- 图表尺寸：800x500 像素（单属性），1200x800 像素（多属性对比）

#### 2.4.6 性能要求

- 懒加载计算：统计值按需计算，不一次性加载全部数据
- 单次统计计算时间：< 5 秒（1 亿点文件，单属性）
- 内存占用：统计过程中峰值内存 < 300MB
- 分布图绘制时间：< 10 秒（1 亿点文件，单属性）

### 2.5 高斯椭球过滤

**功能描述**：根据指定参数的数值或百分比范围条件，过滤掉符合条件的高斯椭球，生成新的 PLY 文件。支持多个参数各自独立的过滤条件，也支持对多个参数统一应用相同的过滤操作。

**详细需求**：

#### 2.5.1 过滤条件类型

| 操作符 | 含义 | 示例 | 说明 |
|--------|------|------|------|
| `>` | 大于某数值 | `opacity>0.1` | 过滤掉 opacity > 0.1 的椭球 |
| `>=` | 大于等于某数值 | `scale_0>=0.5` | 过滤掉 scale_0 >= 0.5 的椭球 |
| `<` | 小于某数值 | `opacity<0.01` | 过滤掉 opacity < 0.01 的椭球 |
| `<=` | 小于等于某数值 | `z<=0` | 过滤掉 z <= 0 的椭球 |
| `==` | 等于某数值 | `rot_0==0` | 过滤掉 rot_0 == 0 的椭球 |
| `!=` | 不等于某数值 | `opacity!=0` | 过滤掉 opacity != 0 的椭球 |
| `>P` | 大于某百分比（分位数） | `opacity>P95` | 过滤掉 opacity > 95% 分位数的椭球 |
| `>=P` | 大于等于某百分比 | `scale_0>=P10` | 过滤掉 scale_0 >= 10% 分位数的椭球 |
| `<P` | 小于某百分比 | `opacity<P5` | 过滤掉 opacity < 5% 分位数的椭球 |
| `<=P` | 小于等于某百分比 | `z<=P50` | 过滤掉 z <= 50% 分位数的椭球 |
| `~` | 介于某数值范围（含边界） | `x~[-10,10]` | 过滤掉 x 在 [-10, 10] 范围内的椭球 |
| `!~` | 不介于某数值范围 | `x!~[-10,10]` | 过滤掉 x 不在 [-10, 10] 范围内的椭球 |
| `~P` | 介于某百分比范围（含边界） | `opacity~P[5,95]` | 过滤掉 opacity 在 5%~95% 分位数之间的椭球 |
| `!~P` | 不介于某百分比范围 | `z!~P[10,90]` | 过滤掉 z 不在 10%~90% 分位数之间的椭球 |

#### 2.5.1.1 间接参数（派生属性）

除了 PLY 文件中的原始属性，过滤还支持以下**间接参数**。这些参数根据高斯椭球的三个缩放值（`scale_0`, `scale_1`, `scale_2`）实时计算，无需存储到文件中。

> **计算基础**：3DGS 中的 `scale_0/1/2` 存储的是对数缩放值，实际半轴长度为 `exp(scale_0)`, `exp(scale_1)`, `exp(scale_2)`。为便于使用，间接参数对原始 scale 值排序后记为 `s1 <= s2 <= s3`（对应半轴 `l1 <= l2 <= l3`）。

| 间接参数名 | 计算方式 | 说明 |
|-----------|---------|------|
| `volume` | `exp(scale_0) * exp(scale_1) * exp(scale_2)` | 高斯椭球体积（成正比） |
| `longest_axis` | `max(exp(scale_0), exp(scale_1), exp(scale_2))` | 最长半轴长度 |
| `shortest_axis` | `min(exp(scale_0), exp(scale_1), exp(scale_2))` | 最短半轴长度 |
| `sphericity` | `l1 / l3`（排序后最短/最长半轴比） | 接近圆球程度：范围 [0,1]，1 为完美球体，越小越扁平/细长 |
| `disceness` | `l1 / l2`（排序后最短/中位半轴比） | 接近圆盘/椭圆饼程度：范围 [0,1]，接近 0 表示一个维度远小于另外两个，呈盘状 |
| `rodness` | `l2 / l3`（排序后中位/最长半轴比） | 接近棒/针状程度：范围 [0,1]，接近 0 表示两个维度远小于第三个，呈棒状 |

**使用示例**：

```bash
# 过滤掉体积极小的高斯椭球（体积 < 5% 分位数）
3dgs-pp filter --filter "volume<P5" scene.ply

# 过滤掉最长轴过大的异常椭球
3dgs-pp filter --filter "longest_axis>P95" scene.ply

# 过滤掉接近完美球体的椭球（sphericity > 0.9）
3dgs-pp filter --filter "sphericity>0.9" scene.ply

# 过滤掉呈扁平圆盘状的椭球（disceness < 0.1）
3dgs-pp filter --filter "disceness<0.1" scene.ply

# 过滤掉呈细长棒状的椭球（rodness < 0.1）
3dgs-pp filter --filter "rodness<0.1" scene.ply

# 组合：保留既不是球体也不是棒状的椭球
3dgs-pp filter --and --filter "sphericity>0.8" --filter "rodness>0.8" scene.ply

# 过滤掉极小且扁平的椭球（体积和圆盘程度同时满足）
3dgs-pp filter --and --filter "volume<P10" --filter "disceness<0.2" scene.ply
```

**与原始属性组合使用**：

间接参数可以与原始属性在过滤表达式中自由组合：

```bash
# 过滤掉不透明度高但体积极小的椭球（可能是噪点）
3dgs-pp filter --and --filter "opacity>0.5" --filter "volume<P5" scene.ply

# 过滤掉位置在远处且形状异常的椭球
3dgs-pp filter --and --filter "z>100" --filter "sphericity<0.1" scene.ply
```

**性能说明**：间接参数在计算时只需要读取 `scale_0, scale_1, scale_2` 三个原始属性，通过 numpy 向量化计算生成派生列，计算开销极低（< 10ms 对于 1 亿点）。

#### 2.5.2 多参数过滤

支持两种指定方式：

**方式一：独立条件（推荐）**

对每个参数单独指定过滤条件，通过多次 `--filter` 参数组合：

```bash
3dgs-pp filter --filter "opacity<P5" --filter "scale_0>P90" scene.ply
```

等价于：过滤掉 `(opacity < P5 分位数) OR (scale_0 > P90 分位数)` 的椭球

**方式二：统一操作**

对多个参数统一应用相同的比较操作：

```bash
3dgs-pp filter --op ">" --threshold 0.1 --attr opacity scale_0 scene.ply
```

等价于：过滤掉 `(opacity > 0.1) OR (scale_0 > 0.1)` 的椭球

#### 2.5.3 逻辑组合

- 多个 `--filter` 条件之间默认使用 **OR** 逻辑（满足任一条件即被过滤）
- 通过 `--and` 标志切换为 **AND** 逻辑（满足所有条件才被过滤）
- 通过 `--keep` 标志反转逻辑（保留符合条件的点，而非过滤掉）

```bash
# OR 逻辑（默认）：过滤掉 opacity<P5 或 scale_0>P90 的椭球
3dgs-pp filter --filter "opacity<P5" --filter "scale_0>P90" scene.ply

# AND 逻辑：仅过滤掉同时满足 opacity<P5 且 scale_0>P90 的椭球
3dgs-pp filter --and --filter "opacity<P5" --filter "scale_0>P90" scene.ply

# 保留模式：保留 opacity>0.1 的椭球（丢弃其余）
3dgs-pp filter --keep --filter "opacity>0.1" scene.ply
```

#### 2.5.4 交互模式

```bash
3dgs-pp filter --interactive scene.ply
```

**交互流程**：

1. 自动进入统计概览模式（复用 stat 功能的统计能力）
2. 用户浏览各属性统计值，确认过滤阈值
3. 输入过滤表达式，实时预览过滤结果（保留/丢弃的点数和比例）
4. 确认或调整过滤条件，支持多轮迭代
5. 最终确认后将结果写入新文件

**交互控制**：

- `a` / `d` 或 `←` / `→`：切换查看属性（复用 stat 功能）
- `f` + `回车`：输入过滤表达式，预览结果
- `F` + `回车`：追加过滤条件（多条件组合）
- `Enter`（在预览模式下）：确认过滤并写入文件
- `Esc`：取消当前过滤条件
- `o`：指定输出文件路径
- `q`：退出交互模式

**预览示例**：

```
过滤条件: opacity<P5
  原始点数: 52,384,129
  将被过滤: 2,619,206 (5.00%)
  将保留:   49,764,923 (95.00%)
  估算输出文件大小: 1.82 GB
  预计处理时间: ~12 秒

  [Enter] 确认写入   [f] 修改条件   [Esc] 取消   [q] 退出
```

#### 2.5.5 输出结果

- 生成新的 PLY 文件，包含过滤后保留的高斯椭球
- 保留所有原始属性数据
- 在 PLY header 中添加注释记录过滤参数
- 默认输出文件命名：`{原文件名}_filtered.ply`
- 支持 `--output FILE` 指定输出路径
- 控制台输出统计信息：
  - 原始点数
  - 被过滤的点数和比例
  - 保留的点数和比例
  - 各条件命中的点数（多条件时）
  - 处理时间

#### 2.5.6 进度反馈

- 百分比分位数计算进度
- 过滤处理进度
- 已处理/总点数
- 估算剩余时间

#### 2.5.7 性能要求

- 分位数计算：按需缓存，同一属性多次使用相同分位数时不重复计算
- 过滤处理速度：> 200 万点/秒
- 内存占用：过滤过程中峰值 < 300MB

### 2.6 高斯椭球下采样

**功能描述**：对 3DGS 模型进行下采样，按照指定百分比或目标数量保留高斯椭球，生成新的 PLY 文件。

**详细需求**：

#### 2.6.1 采样策略

**V1.0 - 均匀采样（优先实现）**：
- 按索引均匀间隔采样：`保留点索引 = i * step`，其中 `step = 原始点数 / 目标点数`
- 简单高效，适合快速预览
- 适用于点分布相对均匀的场景

**可选高级策略（后续版本）**：

**策略 A - 基于不透明度的重要性采样**：
- 优先保留不透明度高的高斯椭球（对渲染质量影响更大）
- 可配置不透明度权重：`score = opacity * scale_factor`
- 适合需要保持视觉质量的场景

**策略 B - 基于体积的采样**：
- 根据高斯椭球的体积（scale 乘积）进行采样
- 优先保留体积大的高斯椭球
- 适合几何结构保持

**策略 C - 空间聚类采样（Voxel-based）**：
- 将空间划分为体素网格
- 每个体素保留一个代表性高斯椭球（不透明度最高或居中）
- 能够更好地保持空间分布

**策略 D - 随机采样**：
- 完全随机选择保留的点
- 可设置随机种子保证可复现性
- 适合数据增强或统计分析

**策略 E - 高斯椭球合并（Gaussian Merging）**：
- 按空间位置将临近的高斯椭球合并成一个新的高斯椭球
- 不是简单保留/删除，而是通过加权平均整合临近高斯的属性
- 能够更好地保持场景的整体视觉效果
- 适用于需要平滑下采样效果的场景

#### 2.6.2 输入参数

支持两种指定方式（二选一）：
- `--ratio R`：保留百分比，R 为 0 到 1 之间的小数（如 `--ratio 0.5` 表示保留 50%）
- `--count N`：保留的高斯椭球数量，N 为正整数

通用参数：
- `--output FILE`：输出文件路径（默认：`{原文件名}_downsampled.ply`）
- `--method NAME`：采样方法（默认：`uniform`）
  - `uniform`：均匀采样
  - `opacity`：基于不透明度采样（可选）
  - `voxel`：体素聚类采样（可选）
  - `random`：随机采样（可选）
  - `merge`：高斯椭球合并（可选）
- `--seed S`：随机种子（仅随机采样时使用）

#### 2.6.3 输出结果

- 生成新的 PLY 文件，包含下采样后的高斯椭球
- 保留所有原始属性数据
- 在 PLY header 中添加注释记录下采样参数
- 控制台输出统计信息：
  - 原始点数
  - 保留点数
  - 压缩比例
  - 处理时间

#### 2.6.4 进度反馈
- 显示采样进度
- 显示已处理/总点数
- 显示采样方法和参数

### 2.7 坐标平移

**功能描述**：将 PLY 文件中所有高斯点的位置坐标（x, y, z）进行平移变换，支持指定具体数值或基于统计量（均值、中值、最小值、最大值、分位数、包围盒中心）自动计算平移量。

**详细需求**：

#### 2.7.1 平移量指定方式

**方式一：具体数值**

直接指定各轴的平移量（单位为原始坐标单位）：

```bash
3dgs-pp translate --x 10.5 --y -3.2 --z 0 scene.ply
```

平移后坐标：`x' = x + 10.5`, `y' = y - 3.2`, `z' = z + 0`

**方式二：统计量自动计算**

基于原始坐标分布的统计量计算平移量，支持以下关键字：

| 关键字 | 计算方式 | 说明 |
|--------|---------|------|
| `mean` | 各轴坐标的算术平均值 | 将数据中心偏移至原点附近 |
| `median` | 各轴坐标的中值（50% 分位数） | 对异常值更鲁棒的中心 |
| `min` | 各轴坐标的最小值 | 将数据下边界平移至原点 |
| `max` | 各轴坐标的最大值 | 将数据上边界平移至原点 |
| `center` | `(min + max) / 2`，包围盒中心 | 基于空间范围的中心 |
| `P<N>` | `<N>`% 分位数（如 `P10`、`P90`） | 按指定分位数计算偏移 |

自动计算模式下的平移逻辑：`x' = x - 参考值_x`，即减去参考值使数据中心化。

**方式三：统一平移量**

通过 `--all` 标志对所有轴应用相同的平移量：

```bash
# 所有轴统一平移 10 个单位
3dgs-pp translate --all 10 scene.ply

# 所有轴统一减去 mean
3dgs-pp translate --all mean scene.ply
```

#### 2.7.2 命令行接口

```bash
3dgs-pp translate [options] <ply_file>
```

参数：

| 参数 | 说明 |
|------|------|
| `--x <val>` | X 轴平移量（数值或 `min`/`max`/`mean`/`median`/`center`/`P<N>`） |
| `--y <val>` | Y 轴平移量（数值或 `min`/`max`/`mean`/`median`/`center`/`P<N>`） |
| `--z <val>` | Z 轴平移量（数值或 `min`/`max`/`mean`/`median`/`center`/`P<N>`） |
| `--all <val>` | 所有轴统一平移量（数值或统计量关键字），与 `--x/--y/--z` 互斥 |
| `--output FILE` | 输出文件路径（默认：`{原文件名}_translated.ply`） |

**混合模式**：`--x/--y/--z` 可以分别指定不同的类型（数值或统计量），未指定的轴默认为不平移（平移量 = 0）：

```bash
# 仅 X 轴平移，YZ 不变
3dgs-pp translate --x mean scene.ply

# X 按具体值，Y 按均值，Z 按中值
3dgs-pp translate --x 10 --y mean --z median scene.ply

# 仅 Y 轴按包围盒中心平移
3dgs-pp translate --y center scene.ply
```

#### 2.7.3 使用示例

```bash
# 将 X 坐标 +10，Y 坐标 -5，Z 不变
3dgs-pp translate --x 10 --y -5 scene.ply

# 以均值为中心，将所有坐标中心化
3dgs-pp translate --all mean scene.ply

# X 轴按均值、Y 轴按中值、Z 轴按包围盒中心分别平移
3dgs-pp translate --x mean --y median --z center scene.ply

# 以 50% 分位数（即中值）为中心
3dgs-pp translate --all P50 scene.ply

# 按 Y 轴 10% 分位数平移，使 10% 的数据位于原点下方
3dgs-pp translate --y P10 scene.ply

# 按最小值平移，将各轴下边界对齐至原点
3dgs-pp translate --all min scene.ply

# 按最大值平移
3dgs-pp translate --x min --y max scene.ply

# 指定输出文件
3dgs-pp translate --all mean --output centered.ply scene.ply
```

#### 2.7.4 输出结果

- 生成新的 PLY 文件，仅修改 x, y, z 坐标值，其他属性保持不变
- 在 PLY header 的注释中记录平移参数和实际平移量：

```
comment translate_x=mean(12.345678)
comment translate_y=median(-8.901234)
comment translate_z=center(25.678901)
```

- 控制台输出平移前后的统计对比：

```
平移参数:
  X: mean (12.345678)
  Y: median (-8.901234)
  Z: center (25.678901)

平移前包围盒:
  X: [-123.456, 156.789]
  Y: [-89.012, 167.890]
  Z: [0.123, 112.345]

平移后包围盒:
  X: [-135.802, 144.443]
  Y: [-79.111, 176.791]
  Z: [-25.556, 86.666]

总处理点数: 52,384,129
处理时间: 1.23 秒
输出文件: scene_translated.ply
```

#### 2.7.5 交互模式

```bash
3dgs-pp translate --interactive scene.ply
```

**交互流程**：

1. 展示各轴坐标的统计概览（min, max, mean, median, center, 分位数）
2. 用户选择平移参考方式（具体数值 / 统计量关键字）
3. 预览平移前后的包围盒变化
4. 确认或调整平移量
5. 写入文件

**交互控制**：

- `x` / `y` / `z`：切换到对应轴的设置
- `+` / `-`：微调平移量
- `n`：切换当前轴为 min
- `x`：切换当前轴为 max（注意：与切换 X 轴同名，通过上下文区分）
- `m`：切换当前轴为 mean
- `d`：切换当前轴为 median
- `c`：切换当前轴为 center
- `p`：输入百分比分位数（如 P10）
- `a`：应用统一平移量到所有轴
- `Enter`：确认并写入文件
- `q`：退出

#### 2.7.6 性能要求

- 统计量预计算时间：< 3 秒（1 亿点文件）
- 坐标平移处理速度：> 300 万点/秒
- 内存占用：平移过程中峰值 < 200MB

### 2.8 轴变换

**功能描述**：对 PLY 文件中所有高斯点的位置坐标（x, y, z）进行轴变换，包括轴对换（镜像）和绕坐标轴 nπ/2 旋转。同时同步变换四元数（rot_0~rot_3）和缩放（scale_0~scale_2），确保高斯椭球朝向和形状与位置变换一致。

**详细需求**：

#### 2.8.1 变换类型

**类型一：轴对换（镜像）**

交换两个坐标轴，可选对各轴附加符号取反。本质是沿着指定平面做镜像反射。

| 变换 | 符号 | 说明 |
|------|------|------|
| `(x,y)→(y,x)` | `swap xy` | 沿 x=y 平面镜像 |
| `(x,y)→(-y,-x)` | `swap -xy` | 沿 x=-y 平面镜像 |
| `(x,z)→(z,x)` | `swap xz` | 沿 x=z 平面镜像 |
| `(x,z)→(-z,-x)` | `swap -xz` | 沿 x=-z 平面镜像 |
| `(y,z)→(z,y)` | `swap yz` | 沿 y=z 平面镜像 |
| `(y,z)→(-z,-y)` | `swap -yz` | 沿 y=-z 平面镜像 |

**类型二：轴自反（镜像）**

对单个坐标轴取反。本质是沿垂直于该轴的平面做镜像反射。

| 变换 | 符号 | 说明 |
|------|------|------|
| `(x,y,z)→(-x,y,z)` | `inv x` | 沿 yz 平面镜像（x 轴自反） |
| `(x,y,z)→(x,-y,z)` | `inv y` | 沿 xz 平面镜像（y 轴自反） |
| `(x,y,z)→(x,y,-z)` | `inv z` | 沿 xy 平面镜像（z 轴自反） |

**类型三：nπ/2 旋转**

绕某一坐标轴旋转 n×90°（n=1,2,3），旋转方向遵循右手定则。

| 变换 | 符号 | 说明 |
|------|------|------|
| `(x,y)→(y,-x)` | `rot z 90` | 绕 z 轴旋转 π/2（90°） |
| `(x,y)→(-x,-y)` | `rot z 180` | 绕 z 轴旋转 π（180°） |
| `(x,y)→(-y,x)` | `rot z 270` | 绕 z 轴旋转 3π/2（270°） |
| `(x,z)→(z,-x)` | `rot y 90` | 绕 y 轴旋转 90° |
| `(x,z)→(-x,-z)` | `rot y 180` | 绕 y 轴旋转 180° |
| `(x,z)→(-z,x)` | `rot y 270` | 绕 y 轴旋转 270° |
| `(y,z)→(z,-y)` | `rot x 90` | 绕 x 轴旋转 90° |
| `(y,z)→(-y,-z)` | `rot x 180` | 绕 x 轴旋转 180° |
| `(y,z)→(-z,y)` | `rot x 270` | 绕 x 轴旋转 270° |

**通用表达式**

对于上述预设符号无法覆盖的组合，支持使用通用映射表达式指定变换。语法为 `new_ax->±old_ax,...`，描述每个新坐标轴来自哪个旧坐标轴（可带符号）：

```
x->y,y->-x,z->z      # 等价于 rot z 90
x->y,y->x,z->z        # 等价于 swap xy
x->-y,y->-x,z->z      # 等价于 swap -xy
x->-x,y->y,z->z       # 等价于 inv x
```

#### 2.8.2 四元数同步变换

轴变换不仅是位置变化，还会改变坐标系的方向。因此必须同步变换高斯椭球的旋转四元数，确保椭球在空间中的朝向与变换后的坐标系一致。

**简化实现**：将轴变换理解为坐标轴重标记。四元数 `rot_0 + rot_1·i + rot_2·j + rot_3·k` 中，实部 `rot_0`（标量 `w`）保持不变，虚部 `(rot_1, rot_2, rot_3)` 的变换规则与坐标 `(x, y, z)` 完全一致——直接对换数值或其符号。

但镜像变换（轴对换、轴自反）还会翻转手性。仅做轴置换和符号翻转，四元数的旋转轴方向是对的，但旋转方向没有镜像。因此**在坐标变换之后，需要额外对四元数取共轭**（即虚部 `(rot_1, rot_2, rot_3)` 全部取反），以将旋转方向也做镜像。

纯旋转（nπ/2）不翻转手性，不需要取共轭。

**轴对换（镜像，需取共轭）：**
| 变换 | 坐标 `(x,y,z)`→ | 四元数 `(w, rot_1, rot_2, rot_3)`→ |
|------|----------------|-------------------------------------|
| `swap xy` | `(y, x, z)` | `(w, -rot_2, -rot_1, -rot_3)` |
| `swap -xy` | `(-y, -x, z)` | `(w, rot_2, rot_1, -rot_3)` |
| `swap xz` | `(z, y, x)` | `(w, -rot_3, -rot_2, -rot_1)` |
| `swap -xz` | `(-z, y, -x)` | `(w, rot_3, rot_2, -rot_1)` |
| `swap yz` | `(x, z, y)` | `(w, -rot_1, -rot_3, -rot_2)` |
| `swap -yz` | `(x, -z, -y)` | `(w, rot_1, rot_3, -rot_2)` |

**轴自反（镜像，需取共轭）：**
| 变换 | 坐标 `(x,y,z)`→ | 四元数 `(w, rot_1, rot_2, rot_3)`→ |
|------|----------------|-------------------------------------|
| `inv x` | `(-x, y, z)` | `(w, rot_1, -rot_2, -rot_3)` |
| `inv y` | `(x, -y, z)` | `(w, -rot_1, rot_2, -rot_3)` |
| `inv z` | `(x, y, -z)` | `(w, -rot_1, -rot_2, rot_3)` |

**nπ/2 旋转（纯旋转，不取共轭）：**
| 变换 | 坐标 `(x,y,z)`→ | 四元数 `(w, rot_1, rot_2, rot_3)`→ |
|------|----------------|-------------------------------------|
| `rot z 90` | `(y, -x, z)` | `(w, rot_2, -rot_1, rot_3)` |
| `rot z 180` | `(-x, -y, z)` | `(w, -rot_1, -rot_2, rot_3)` |
| `rot z 270` | `(-y, x, z)` | `(w, -rot_2, rot_1, rot_3)` |
| `rot y 90` | `(z, y, -x)` | `(w, rot_3, rot_2, -rot_1)` |
| `rot y 180` | `(-x, y, -z)` | `(w, -rot_1, rot_2, -rot_3)` |
| `rot y 270` | `(-z, y, x)` | `(w, -rot_3, rot_2, rot_1)` |
| `rot x 90` | `(x, z, -y)` | `(w, rot_1, rot_3, -rot_2)` |
| `rot x 180` | `(x, -y, -z)` | `(w, rot_1, -rot_2, -rot_3)` |
| `rot x 270` | `(x, -z, y)` | `(w, rot_1, -rot_3, rot_2)` |

> **原理**：虚部 `(rot_1, rot_2, rot_3)` 分列在 x, y, z 轴上，其轴变换与坐标向量相同。镜像变换（det = -1）翻转手性，需额外取四元数共轭（虚部全取反）以镜像旋转方向；纯旋转（det = +1）不翻转手性，不需共轭。

#### 2.8.3 缩放同步变换

`scale_0`, `scale_1`, `scale_2` 对应三个轴向的高斯椭球大小（对数尺度），在轴变换时需要同步变换以确保椭球形状与变换后的坐标系一致。

**变换规则**：与坐标 `(x, y, z)` 做相同的**轴置换**，但**不取符号**（scale 为大小，始终为正）。即 `scale_i` 的来源轴与坐标第 `i` 分量的来源轴相同，但不考虑符号取反。

| 变换 | `scale_0/1/2`→ | 说明 |
|------|----------------|------|
| `inv x` | `(s0, s1, s2)` | 仅 x 取反，不涉及轴置换，scale 不变 |
| `inv y` | `(s0, s1, s2)` | 仅 y 取反，不涉及轴置换，scale 不变 |
| `inv z` | `(s0, s1, s2)` | 仅 z 取反，不涉及轴置换，scale 不变 |
| `swap xy` | `(s1, s0, s2)` | x↔y 轴对换 |
| `swap xz` | `(s2, s1, s0)` | x↔z 轴对换 |
| `swap yz` | `(s0, s2, s1)` | y↔z 轴对换 |
| `rot z 90` | `(s1, s0, s2)` | x 来自 y 轴，y 来自 x 轴 |
| `rot y 90` | `(s2, s1, s0)` | x 来自 z 轴，z 来自 x 轴 |
| `rot x 90` | `(s0, s2, s1)` | y 来自 z 轴，z 来自 y 轴 |

> **原理**：scale 存储的是对数尺度，表示椭球在每个轴向上的大小，不带有方向信息。因此轴变换时只需做轴置换（scale_0/1/2 分别对应 x/y/z 轴），不需要考虑符号翻转。`inv` 类变换不改变轴之间的对应关系，scale 保持不变。

#### 2.8.4 命令行接口

```bash
3dgs-pp transform [options] <ply_file>
```

参数：

| 参数 | 说明 |
|------|------|
| `--swap AXES` | 轴对换（镜像），如 `xy`、`nxy`、`xz`、`nyz` 等（`n` 前缀表示负号） |
| `--inv AXIS` | 轴自反（镜像），AXIS 为 `x`/`y`/`z`，如 `inv x` |
| `--rot AXIS ANGLE` | 绕轴旋转，AXIS 为 `x`/`y`/`z`，ANGLE 为 `90`/`180`/`270` |
| `--transform EXPR` | 通用映射表达式，如 `x->y,y->-x,z->z` |
| `--interactive` | 进入交互模式 |
| `--output FILE` | 输出文件路径（默认：`{原文件名}_transformed.ply`） |

> `--swap`、`--inv`、`--rot`、`--transform` 四选一，不可同时指定。

**使用示例**：

```bash
# 沿 x=y 平面镜像
3dgs-pp transform --swap xy scene.ply

# 沿 x=-y 平面镜像
3dgs-pp transform --swap nxy scene.ply

# 沿 yz 平面镜像（x 轴自反）
3dgs-pp transform --inv x scene.ply

# 沿 xy 平面镜像（z 轴自反）
3dgs-pp transform --inv z scene.ply

# 绕 z 轴旋转 90°
3dgs-pp transform --rot z 90 scene.ply

# 绕 z 轴旋转 180°
3dgs-pp transform --rot z 180 scene.ply

# 绕 x 轴旋转 270°
3dgs-pp transform --rot x 270 scene.ply

# 通用表达式：绕 z 轴旋转 90°
3dgs-pp transform --transform "x->y,y->-x,z->z" scene.ply

# 交互模式
3dgs-pp transform --interactive scene.ply
```

#### 2.8.5 输出结果

- 生成新的 PLY 文件，修改 x, y, z 坐标值、rot_0~rot_3 四元数、scale_0~scale_2 缩放，其他属性保持不变
- 在 PLY header 的注释中记录变换参数：

```
comment transform=swap_xy
comment transform_matrix=[[0,1,0],[1,0,0],[0,0,1]]
```

- 控制台输出变换前后的统计对比：

```
变换参数: swap xy (沿 x=y 平面镜像)

变换前包围盒:
  X: [-123.456, 156.789]
  Y: [-89.012, 167.890]
  Z: [0.123, 112.345]

变换后包围盒:
  X: [-89.012, 167.890]
  Y: [-123.456, 156.789]
  Z: [0.123, 112.345]

总处理点数: 52,384,129
处理时间: 1.56 秒
输出文件: scene_transformed.ply
```

#### 2.8.6 交互模式

```bash
3dgs-pp transform --interactive scene.ply
```

**交互流程**：

1. 展示当前点云的包围盒和各轴统计概览
2. 选择变换类型（轴对换 / 旋转 / 通用表达式）
3. 预览变换前后的包围盒变化
4. 确认或调整变换参数
5. 写入文件

**交互控制**：

- `s`：轴对换模式，选择轴对（xy/xz/yz）和符号（正/负）
- `r`：旋转模式，选择旋转轴（x/y/z）和角度（90/180/270）
- `t`：输入通用映射表达式
- `Enter`：确认并写入文件
- `q`：退出

#### 2.8.7 性能要求

- 变换处理速度：> 300 万点/秒
- 内存占用：变换过程中峰值 < 200MB

### 2.9 空间裁剪

**功能描述**：按照指定的坐标区域从 PLY 文件中裁剪出指定区域的高斯点，或者保留剩余的高斯点（即反向选择）。支持两点式（轴对齐包围盒）和八点式（凸六面体）两种区域指定方式。

**详细需求**：

#### 2.9.1 区域指定方式

**方式一：两点式（轴对齐包围盒 AABB）**

通过两个对角点（`p1` 和 `p2`）定义一个轴对齐的立方体区域。两点之间的沿坐标轴范围内的点即为选中区域。

- `p1`：最小角点 (min_x, min_y, min_z)
- `p2`：最大角点 (max_x, max_y, max_z)
- 选中区域：`min_x <= x <= max_x AND min_y <= y <= max_y AND min_z <= z <= max_z`

**方式二：八点式（凸六面体）**

通过八个顶点（`p1`~`p8`）定义一个凸六面体（convex hexahedron），六面体内部范围的点即为选中区域。

- 八个点必须构成一个凸六面体，即 6 个面、12 条边、8 个顶点
- 点的顺序不影响凸六面体判定（通过凸包自动计算面连接关系）
- 使用 `scipy.spatial.ConvexHull` 计算 6 个面的方程，逐点判断是否在所有面的内侧
- 选中区域：点位于凸六面体内部（含边界，容差 1e-10）

#### 2.9.2 裁剪模式

| 模式 | 说明 |
|------|------|
| 默认（保留选中区） | 仅保留指定区域内的点，丢弃区域外的点 |
| `--outside`（保留剩余区） | 保留指定区域外的点，丢弃区域内的点 |

#### 2.9.3 命令行接口

```bash
3dgs-pp crop [--p1 x,y,z --p2 x,y,z | --p1 ... --p8 ...] [--outside] [--output FILE] <ply_file>
```

参数：

| 参数 | 说明 |
|------|------|
| `--p1 x,y,z` | 点 1，两点式时为最小角点，八点式时为顶点 1 |
| `--p2 x,y,z` | 点 2，两点式时为最大角点，八点式时为顶点 2 |
| `--p3~--p8 x,y,z` | 八点式时的顶点 3~8 |
| `--outside` | 反向选择：保留区域外的点（默认保留区域内的点） |
| `--output FILE` | 输出文件路径（默认：`{原文件名}_cropped.ply`） |

**使用示例**：

```bash
# 两点式：裁剪出 AABB 区域内的点
3dgs-pp crop --p1 -10,-5,0 --p2 10,5,20 scene.ply

# 两点式：保留 AABB 区域外的点
3dgs-pp crop --p1 -10,-5,0 --p2 10,5,20 --outside scene.ply

# 八点式：裁剪出凸六面体内的点
3dgs-pp crop \
  --p1 0,0,0 --p2 10,0,0 --p3 10,10,0 --p4 0,10,0 \
  --p5 2,2,20 --p6 8,2,20 --p7 8,8,20 --p8 2,8,20 \
  scene.ply

# 指定输出文件
3dgs-pp crop --p1 -10,-5,0 --p2 10,5,20 --output region.ply scene.ply
```

#### 2.9.4 输出结果

- 生成新的 PLY 文件，包含裁剪后的高斯椭球
- 保留所有原始属性数据
- 在 PLY header 中添加注释记录裁剪参数：

```
comment crop mode=inside p1=(-10,-5,0) p2=(10,5,20)
comment cropped points: kept=1,234,567 of 52,384,129
```

- 控制台输出统计信息：
  - 原始点数
  - 区域内 / 区域外点数
  - 保留点数
  - 处理时间

#### 2.9.5 性能要求

- 两点式裁剪速度：> 300 万点/秒
- 八点式裁剪速度：> 100 万点/秒（凸包计算 + 逐点面判定）
- 内存占用：峰值 < 300MB

### 2.10 空间填充

**功能描述**：将一个 PLY 文件（源文件）中的高斯点填充到另一个 PLY 文件（目标文件）的指定区域中。通过相对偏移量将源点云与目标区域对齐，仅将落入目标区域内的源点填充进去，超出目标区域的源点忽略。

**详细需求**：

#### 2.10.1 填充流程

1. 读取源 PLY 文件的所有高斯点坐标
2. 对所有源点坐标应用相对偏移量 `(dx, dy, dz)`：`x' = x + dx`, `y' = y + dy`, `z' = z + dz`
3. 判断偏移后的源点哪些落在目标区域内（两点式或八点式）
4. 仅保留落入目标区域内的源点
5. 将这些源点追加到目标 PLY 文件中，生成新的合并 PLY 文件

#### 2.10.2 区域指定方式

同裁剪功能（见 2.9.1），支持两点式（AABB）和八点式（凸六面体）：

- **两点式**：`--p1` 和 `--p2` 定义目标区域的对角点
- **八点式**：`--p1`~`--p8` 定义目标区域的凸六面体

#### 2.10.3 偏移量

- 通过 `--offset dx,dy,dz` 指定相对偏移量
- 偏移量应用于源文件的所有点坐标
- 偏移量默认为 `0,0,0`（不偏移）

#### 2.10.4 属性兼容性

- 源 PLY 和目标 PLY 必须具有相同的属性结构（相同的属性列表和数据类型）
- 如果属性不兼容，提示错误并拒绝执行
- 填充后保留所有原始属性数据

#### 2.10.5 命令行接口

```bash
3dgs-pp fill --source <source.ply> [--p1 x,y,z --p2 x,y,z | --p1 ... --p8 ...] [--offset dx,dy,dz] [--output FILE] <target.ply>
```

参数：

| 参数 | 说明 |
|------|------|
| `--source FILE` | **必需**。源 PLY 文件路径 |
| `--p1 x,y,z` | 目标区域点 1 |
| `--p2 x,y,z` | 目标区域点 2（两点式时为最大角点） |
| `--p3~--p8 x,y,z` | 八点式时的顶点 3~8 |
| `--offset dx,dy,dz` | 相对偏移量，应用于源点（默认：`0,0,0`） |
| `--output FILE` | 输出文件路径（默认：`{目标文件名}_filled.ply`） |

**使用示例**：

```bash
# 两点式：将 source.ply 填充到 target.ply 的 AABB 区域
3dgs-pp fill --source source.ply --p1 -10,-5,0 --p2 10,5,20 target.ply

# 带偏移量填充
3dgs-pp fill --source source.ply --p1 -10,-5,0 --p2 10,5,20 --offset 5,0,0 target.ply

# 八点式目标区域
3dgs-pp fill \
  --source source.ply \
  --p1 0,0,0 --p2 10,0,0 --p3 10,10,0 --p4 0,10,0 \
  --p5 2,2,20 --p6 8,2,20 --p7 8,8,20 --p8 2,8,20 \
  target.ply

# 不偏移，直接填充目标区域
3dgs-pp fill --source source.ply --p1 -50,-50,-50 --p2 50,50,50 target.ply
```

#### 2.10.6 输出结果

- 生成新的 PLY 文件，包含目标 PLY 的所有点 + 落入目标区域的源点
- 在 PLY header 中添加注释记录填充参数：

```
comment fill source=source.ply offset=(5,0,0) p1=(-10,-5,0) p2=(10,5,20)
comment filled: added=1,234 of 10,000 source points
```

- 控制台输出统计信息：
  - 目标文件原始点数
  - 源文件总点数
  - 落入目标区域的源点数（即新增点数）
  - 合并后总点数
  - 处理时间

#### 2.10.7 性能要求

- 属性兼容性检查：< 10ms
- 填充处理速度：> 100 万点/秒
- 内存占用：峰值 < 500MB（源 + 目标同时加载时）

## 3. 非功能需求

### 3.1 性能需求
- 元数据读取时间：< 1 秒（1000 万点文件）
- 单页数据加载时间：< 100ms
- 分块处理速度：> 100 万点/秒
- 过滤处理速度：> 200 万点/秒
- 内存占用峰值：< 500MB（处理 1 亿点文件时）

### 3.2 兼容性需求
- 支持 Python 3.8+
- 支持 Linux/macOS/Windows
- 兼容标准 3DGS PLY 格式

### 3.3 易用性需求
- 清晰的命令行帮助信息
- 友好的错误提示
- 详细的使用文档和示例

## 4. 命令行接口设计

### 4.1 主命令
```bash
3dgs-pp <command> [options] <ply_file>
```

### 4.2 子命令

#### 4.2.1 info - 查看文件信息
```bash
3dgs-pp info <ply_file>
```
输出：
- 文件路径
- 总点数
- 属性列表
- 包围盒信息（min/max x/y/z）

#### 4.2.2 view - 终端浏览
```bash
3dgs-pp view [--page-size N] [--full] <ply_file>
```
选项：
- `--page-size`：每页显示记录数（默认 20）
- `--full`：默认展开完整属性

#### 4.2.3 split - 空间分块
```bash
3dgs-pp split [--output-dir DIR] <split_spec> <ply_file>
```
参数：
- `split_spec`：分块规格，如 `2*3*2`
- `--output-dir`：输出目录（默认与输入文件同目录）

示例：
```bash
3dgs-pp split 2*3*2 scene.ply
3dgs-pp split --output-dir ./blocks 4*4*4 scene.ply
```

#### 4.2.4 filter - 高斯椭球过滤

```bash
3dgs-pp filter [--filter "expr" ...] [--and] [--keep] [--interactive] [--output FILE] <ply_file>
```

参数：
- `--filter "expr"`：过滤表达式，可多次指定
  - 数值比较：`attr>value`, `attr<=value`, `attr~[min,max]`, `attr!~[min,max]`
  - 百分比比较：`attr>P95`, `attr<=P5`, `attr~P[5,95]`, `attr!~P[10,90]`
- `--and`：多条件使用 AND 逻辑（默认 OR）
- `--keep`：反转逻辑，保留符合条件的点（默认丢弃）
- `--interactive`：进入交互模式
- `--output FILE`：输出文件路径（默认：`{原文件名}_filtered.ply`）

示例：
```bash
# 过滤掉 opacity 小于 5% 分位数的椭球
3dgs-pp filter --filter "opacity<P5" scene.ply

# 过滤掉 opacity > 0.1 或 scale_0 < P10 的椭球（OR 逻辑）
3dgs-pp filter --filter "opacity>0.1" --filter "scale_0<P10" scene.ply

# 仅过滤掉同时满足 opacity < 0.01 且 z > 100 的椭球（AND 逻辑）
3dgs-pp filter --and --filter "opacity<0.01" --filter "z>100" scene.ply

# 保留 opacity > P5 的椭球（丢弃其余）
3dgs-pp filter --keep --filter "opacity>P5" scene.ply

# 过滤掉 x 不在 [-50, 50] 范围内的椭球
3dgs-pp filter --filter "x!~[-50,50]" scene.ply

# 交互模式
3dgs-pp filter --interactive scene.ply
```

#### 4.2.5 downsample - 下采样
```bash
3dgs-pp downsample [--ratio R | --count N] [--method NAME] [--output FILE] <ply_file>
```

参数：
- `--ratio R`：保留比例，0 < R ≤ 1（如 0.5 表示保留 50%）
- `--count N`：保留的高斯椭球数量
- `--method NAME`：采样方法（默认：uniform）
  - `uniform`：均匀采样
  - `opacity`：基于不透明度采样
  - `voxel`：体素聚类采样
  - `random`：随机采样
  - `merge`：高斯椭球合并
- `--output FILE`：输出文件路径
- `--seed S`：随机种子（仅随机采样）

示例：
```bash
# 按比例下采样（保留 50%）
3dgs-pp downsample --ratio 0.5 scene.ply

# 按数量下采样（保留 10000 个）
3dgs-pp downsample --count 10000 scene.ply

# 指定采样方法和输出文件
3dgs-pp downsample --ratio 0.3 --method opacity --output scene_small.ply scene.ply
```

#### 4.2.6 stat - 统计概览
```bash
3dgs-pp stat [--attr NAME] [--plot] [--output-dir DIR] [--type TYPE] <ply_file>
```
参数：
- `--attr NAME`：默认统计的属性（默认：opacity）
- `--all`：直接展示所有属性的关键统计值一览
- `--plot`：绘制分布图并保存
- `--output-dir DIR`：统计报告和分布图的输出目录（默认与 PLY 文件同目录）
- `--type TYPE`：图表类型（默认：histogram），可选：`histogram` / `box` / `violin`

示例：
```bash
# 交互式统计模式
3dgs-pp stat scene.ply

# 指定默认查看的属性
3dgs-pp stat --attr x scene.ply

# 非交互模式：展示所有属性概览
3dgs-pp stat --all scene.ply

# 绘制 opacity 分布图并保存到指定目录
3dgs-pp stat --attr opacity --plot --output-dir ./plots scene.ply

# 批量绘制核心属性箱线图
3dgs-pp stat --all --plot --type box --output-dir ./charts scene.ply
```

#### 4.2.7 translate - 坐标平移

```bash
3dgs-pp translate [--x VAL] [--y VAL] [--z VAL] [--all VAL] [--interactive] [--output FILE] <ply_file>
```

参数：
- `--x VAL`：X 轴平移量（数值或 `min`/`max`/`mean`/`median`/`center`/`P<N>`）
- `--y VAL`：Y 轴平移量（数值或 `min`/`max`/`mean`/`median`/`center`/`P<N>`）
- `--z VAL`：Z 轴平移量（数值或 `min`/`max`/`mean`/`median`/`center`/`P<N>`）
- `--all VAL`：所有轴统一平移量，与 `--x/--y/--z` 互斥
- `--interactive`：进入交互模式
- `--output FILE`：输出文件路径（默认：`{原文件名}_translated.ply`）

示例：
```bash
# 具体数值平移
3dgs-pp translate --x 10 --y -5 scene.ply

# 按统计量平移
3dgs-pp translate --all mean scene.ply
3dgs-pp translate --x mean --y median --z center scene.ply

# 混合模式
3dgs-pp translate --x 10 --y mean --z P50 scene.ply

# 交互模式
3dgs-pp translate --interactive scene.ply
```

#### 4.2.8 transform - 轴变换

```bash
3dgs-pp transform [--swap AXES] [--rot AXIS ANGLE] [--transform EXPR] [--interactive] [--output FILE] <ply_file>
```

参数：
- `--swap AXES`：轴对换（镜像），AXES 为 `xy`/`nxy`/`xz`/`nxz`/`yz`/`nyz`（`n` 前缀表示负号）
- `--inv AXIS`：轴自反（镜像），AXIS 为 `x`/`y`/`z`
- `--rot AXIS ANGLE`：绕坐标轴旋转，AXIS 为 `x`/`y`/`z`，ANGLE 为 `90`/`180`/`270`
- `--transform EXPR`：通用映射表达式，如 `x->y,y->-x,z->z`
- `--interactive`：进入交互模式
- `--output FILE`：输出文件路径（默认：`{原文件名}_transformed.ply`）

> `--swap`、`--inv`、`--rot`、`--transform` 四选一，不可同时指定。

示例：
```bash
# 沿 x=y 平面镜像
3dgs-pp transform --swap xy scene.ply

# 沿 x=-y 平面镜像
3dgs-pp transform --swap nxy scene.ply

# 沿 yz 平面镜像（x 轴自反）
3dgs-pp transform --inv x scene.ply

# 绕 z 轴旋转 90°
3dgs-pp transform --rot z 90 scene.ply

# 绕 z 轴旋转 180°
3dgs-pp transform --rot z 180 scene.ply

# 绕 x 轴旋转 270°
3dgs-pp transform --rot x 270 scene.ply

# 通用表达式
3dgs-pp transform --transform "x->y,y->-x,z->z" scene.ply

# 交互模式
3dgs-pp transform --interactive scene.ply
```

## 5. 技术架构

### 5.1 核心模块

```
3dgs-pp/
├── ply/
│   ├── __init__.py
│   ├── header.py       # PLY 头解析
│   ├── reader.py       # 懒加载读取器
│   └── writer.py       # PLY 写入器
├── cli/
│   ├── __init__.py
│   ├── info.py         # info 命令
│   ├── view.py         # view 命令（终端 UI）
│   ├── split.py        # split 命令
│   ├── stat.py         # stat 命令（终端 UI）
│   ├── filter.py       # filter 命令（终端 UI）
│   ├── downsample.py   # downsample 命令
│   ├── translate.py    # translate 命令（终端 UI）
│   └── transform.py    # transform 命令（终端 UI）
├── core/
│   ├── __init__.py
│   ├── bounds.py       # 包围盒计算
│   ├── partition.py    # 空间分块
│   ├── stats.py        # 统计分析
│   ├── filter.py       # 高斯椭球过滤
│   ├── downsampler.py  # 下采样算法
│   ├── translate.py    # 坐标平移
│   └── transform.py    # 轴变换
└── main.py
```

### 5.2 关键技术点

1. **懒加载实现**：基于文件偏移量索引，使用 `seek()` 按需读取
2. **终端 UI**：使用 `curses` 或 `rich` 库实现交互式界面
3. **进度条**：使用 `tqdm` 库展示处理进度
4. **统计分析**：使用 `numpy.percentile` 和 `numpy.mean/std` 高效计算，通过 `np.memmap` 避免全量加载
5. **过滤引擎**：解析过滤表达式，支持数值和百分比分位数条件，通过布尔掩码实现多条件 OR/AND 组合
7. **轴变换**：坐标与四元数均通过直接对换轴数值（或其负值）实现，无需通用矩阵乘法。`scale_0/1/2` 同步做轴置换但不取符号（size 为标量）。例如 `(x,y)→(-y,x)` 对应四元数 `(rot_0,rot_1,rot_2,rot_3)→(rot_0,-rot_1,rot_2,rot_3)`，`scale_0/1/2 → scale_1/0/2`。仅支持轴对换和 nπ/2 旋转，但速度极快。

6. **下采样算法**：
   - 均匀采样：索引步进算法
   - 重要性采样：基于不透明度/体积的权重排序
   - 体素采样：空间网格划分 + 局部最优选择

### 5.3 下采样算法说明

#### 均匀采样（Uniform Sampling）
**原理**：按固定间隔从原始点云中选取点
- 计算采样步长：`step = original_count / target_count`
- 选取索引：`i * step` 向下取整
- **优点**：O(N) 时间复杂度，实现简单，速度极快
- **缺点**：可能丢失重要特征，不适合点密度变化大的场景

#### 基于不透明度采样（Opacity-based Sampling）
**原理**：优先保留不透明度高的高斯椭球
- 计算每个点的重要性分数：`score = opacity`
- 按分数降序排序，选取前 N 个
- **优点**：保留视觉上更重要的点，渲染质量下降更平缓
- **缺点**：需要 O(N log N) 排序时间，可能破坏空间分布均匀性

#### 体素聚类采样（Voxel-based Sampling）
**原理**：空间划分后每个体素保留代表点
1. 根据点云边界和目标点数计算体素大小
2. 将每个点分配到对应体素
3. 每个体素中选择不透明度最高的点作为代表
- **优点**：保持空间分布均匀性，同时考虑重要性
- **缺点**：内存占用较高（需要构建体素索引），实现较复杂

#### 随机采样（Random Sampling）
**原理**：随机选择 N 个点保留
- 使用 Fisher-Yates shuffle 或 reservoir sampling
- 可设置随机种子保证可复现
- **优点**：无偏采样，适合统计分析
- **缺点**：可能丢失重要特征

#### 高斯椭球合并（Gaussian Merging）
**原理**：将空间临近的高斯椭球合并为一个新的高斯椭球
1. 使用体素网格或 K-Means 聚类将空间划分为簇，目标簇数量约等于目标高斯数
2. 每个簇内的多个高斯椭球合并为一个新高斯
3. 权重基于不透明度和体积计算重要性

**重要**：加权平均不适用于所有参数。控制体积的参数（缩放和旋转）必须用协方差矩阵融合，确保合并后的高斯椭球能填满原来所有高斯椭球共同占据的空间，否则视觉上会出现空洞。

**合并公式**：
- **位置**：加权平均：`x_merged = Σ(w_i * x_i) / Σ(w_i)`
- **不透明度**：加权求和：`opacity_merged = Σ(w_i * opacity_i)` （clamp 到合理范围）
- **缩放 + 旋转**：协方差矩阵融合（**不可简单加权平均**）：
  - 对每个高斯椭球，由 scale 和 rot 构造协方差矩阵 `Σ_i = R_i * diag(exp(scale_i))² * R_i^T`
  - 融合协方差：`Σ_merged = Σ(w_i * (Σ_i + (μ_i - μ_merged)(μ_i - μ_merged)^T))`
    - 第一项：高斯椭球自身的体积散布
    - 第二项：各高斯椭球中心相对于合并后中心的位移散布
  - 对 `Σ_merged` 进行特征分解（Eigendecomposition），提取新的 scale（特征值开方取 log）和 rot（特征向量构成旋转矩阵转四元数）
- **球面调和系数**：加权平均

**权重计算**：`w_i = opacity_i * volume_i`（`volume_i = exp(scale_0_i) * exp(scale_1_i) * exp(scale_2_i)`）

- **优点**：
  - 平滑过渡，不会出现突然的视觉跳变
  - 协方差融合确保合并后高斯覆盖原有全部空间，不会出现空洞
  - 更好地保持场景整体外观
  - 适合大幅度下采样时仍能保持较好视觉质量
- **缺点**：
  - 计算复杂度较高（需要矩阵运算和特征分解）
  - 内存占用较大
  - 可能模糊细节

## 6. 验收标准

### 6.1 功能验收
- [ ] 能正确读取 3DGS PLY 文件并显示元数据
- [ ] 懒加载模式下内存占用符合要求
- [ ] 终端浏览支持所有指定的交互命令
- [ ] 分块导出功能正确生成指定数量的 PLY 文件
- [ ] 分块后的点云可以重新合并恢复原始数据
- [ ] 均匀下采样功能正常工作
- [ ] 高斯椭球合并下采样功能正常工作
- [ ] 统计概览功能正确计算各统计指标（Max/Min/Mean/Median/Percentiles）
- [ ] 统计概览支持快捷键切换属性
- [ ] 统计概览支持单属性详细和多属性对比两种展示模式
- [ ] 统计输出文件格式正确，数值精度合理
- [ ] `o` 快捷键能正确保存当前统计信息到文本文件
- [ ] 分布图绘制正确（直方图+KDE、箱线图、小提琴图）
- [ ] 分布图文件正确保存到指定目录，命名格式符合规范
- [ ] `p` / `P` 快捷键能触发单属性 / 批量分布图绘制
- [ ] 过滤功能正确解析数值比较表达式（`>`, `>=`, `<`, `<=`, `==`, `!=`）
- [ ] 过滤功能正确解析百分比分位数表达式（`>P`, `>=P`, `<P`, `<=P`）
- [ ] 过滤功能正确解析范围表达式（`~`, `!~`, `~P`, `!~P`）
- [ ] 多条件过滤的 OR 和 AND 逻辑正确
- [ ] `--keep` 反转逻辑功能正确
- [ ] 交互模式过滤预览显示正确的保留/丢弃点数和比例
- [ ] 过滤输出文件格式正确，保留所有原始属性数据
- [ ] 支持按比例和按数量两种方式指定下采样目标
- [ ] 下采样输出文件格式正确，可被其他工具读取
- [ ] 坐标平移功能正确应用具体数值平移
- [ ] 坐标平移功能正确解析统计量关键字（`min`/`max`/`mean`/`median`/`center`/`P<N>`）
- [ ] `--all` 统一平移量功能正确应用到各轴
- [ ] `--x/--y/--z` 混合模式支持数值与统计量混合指定
- [ ] 平移输出文件仅修改 x/y/z 坐标，其他属性保持不变
- [ ] 平移输出文件的 PLY header 注释记录平移参数和实际平移量
- [ ] 控制台输出平移前后的包围盒对比信息
- [ ] 交互模式正确展示统计概览并支持平移预览
- [ ] 轴变换 `--swap` 正确执行轴对换（镜像）变换
- [ ] 轴变换 `--inv` 正确执行轴自反（镜像）变换
- [ ] 轴变换 `--rot` 正确执行绕轴 nπ/2 旋转
- [ ] 轴变换 `--transform` 正确解析通用映射表达式
- [ ] 轴变换同步更新四元数（rot_0~rot_3），确保椭球朝向正确（镜像取共轭，纯旋转不取共轭）
- [ ] 轴变换同步更新 scale_0/1/2，与坐标轴置换一致且不取符号
- [ ] 轴变换输出文件仅修改 x/y/z 坐标、四元数和 scale_0/1/2，其他属性保持不变
- [ ] 轴变换输出文件的 PLY header 注释记录变换参数和变换矩阵
- [ ] 控制台输出变换前后的包围盒对比信息
- [ ] 交互模式支持轴对换、旋转、通用表达式三种方式并支持预览

### 6.2 性能验收
- [ ] 1000 万点文件元数据读取 < 1 秒
- [ ] 分块处理速度 > 100 万点/秒
- [ ] 处理 1 亿点文件时内存峰值 < 500MB
- [ ] 过滤处理速度 > 200 万点/秒
- [ ] 均匀下采样速度 > 200 万点/秒
- [ ] 基于不透明度下采样速度 > 50 万点/秒
- [ ] 高斯椭球合并下采样速度 > 10 万点/秒
- [ ] 单属性统计计算时间 < 5 秒（1 亿点文件）
- [ ] 统计模式内存峰值 < 300MB（1 亿点文件）
- [ ] 坐标平移统计量预计算 < 3 秒（1 亿点文件）
- [ ] 坐标平移处理速度 > 300 万点/秒
- [ ] 平移模式内存峰值 < 200MB（1 亿点文件）
- [ ] 轴变换处理速度 > 300 万点/秒
- [ ] 轴变换模式内存峰值 < 200MB（1 亿点文件）
- [ ] 两点式裁剪功能正确选出 AABB 区域内的点
- [ ] 两点式裁剪功能正确保留 AABB 区域外的点（`--outside`）
- [ ] 八点式裁剪功能正确选出凸六面体内的点
- [ ] 裁剪输出文件仅包含/排除指定区域内的点，其他属性保持不变
- [ ] 裁剪输出文件的 PLY header 注释记录裁剪参数
- [ ] 填充功能正确将源 PLY 中的点填充到目标区域
- [ ] 填充功能正确应用偏移量
- [ ] 填充功能仅保留落入目标区域内的源点，超出部分忽略
- [ ] 填充功能检测源和目标 PLY 属性兼容性
- [ ] 填充输出文件包含目标 PLY 的所有点 + 符合条件的新点
- [ ] 填充输出文件的 PLY header 注释记录填充参数

## 7. 后续规划（可选）

- 支持可视化预览窗口
- 支持多种分块策略（八叉树、K-D 树等）
- 支持分块并行处理
- 实现更高级的下采样算法（高斯椭球合并、基于不透明度、体素聚类等）
- 支持下采样质量评估指标（PSNR、LPIPS 等）
- 支持合并多个分块文件
- 支持点云属性编辑和批量修改
